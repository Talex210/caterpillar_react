let initialState = {
    nameApp: 'Snake',
    life: 3,
    snakeLength: 3
}

export const headerReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}