let startState = {
    nameApp: 'Snake',
    life: 3,
    snakeLength: 3
}

export const headerReducer = (state = startState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}